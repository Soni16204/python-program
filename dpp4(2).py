#Define a function find_maximum(a, b) that accepts two numbers and returns the large number
def find_maximum(a, b):
    if a > b:
        return a   
    else:
        return b
result = find_maximum(10, 20)
print(result)  
